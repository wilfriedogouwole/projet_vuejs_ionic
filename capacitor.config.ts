import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'concept_tagging',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
